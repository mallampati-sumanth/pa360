# Django
DJANGO_SECRET_KEY=dev-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_SETTINGS_MODULE=project_config.settings.development

# Use SQLite for local development to avoid Snowflake costs if preferred
# Comment out Snowflake vars to fallback to SQLite (requires settings support)
# SNOWFLAKE_ACCOUNT=...
